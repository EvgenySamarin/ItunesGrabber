/**
 * Contains classes which provides work with remote data sources
 * <p>Hierarchy is represented following template:</p>
 *
 * <p>Remote package: - defines of remote classes which interact with REST API. should be named
 * according to the subject area</p>
 * <ul>
 *     <li>Entity package - defines of data classes which would be returned from REST API like
 *     result of requests</li>
 *     <li>Body package - defines of data classes which would be used like body parameters for
 *     request. This is optional package</li>
 * </ul>
 *
 * @author EvgenySamarin
 * @since 20200314 v1
 */
package com.example.itunesgrabber.model.remote;